# Interview ammo — harvested lines

One STAR-ready sentence per day, appended at the bottom. Use these verbatim in interviews.

- 2026-07-22 — "I designed a core-banking ledger in DuckDB with a hidden ground-truth label table, so my detection stack's precision and recall could be measured exactly — something real FIUs can never do with production data."
- 2026-07-22 — "I hand-crafted a synthetic banking week for 25 customer archetypes — salaries, rent, NACH EMIs, UPI P2P, merchant footfall, GST — realistic enough that UPI came out at 80% of transactions by count, matching real Indian payment mix, without my tuning for it."
- 2026-07-23 — "When my test suite hit 5 minutes, I didn't rewrite the database layer — I found that 9 tests were redundantly reseeding the same data and fixed the test fixtures instead, cutting runtime by 60% with a one-line change. Profile before you optimize."
- 2026-07-26 — "My CI failed on a diff I hadn't touched — a linter upgrade had silently widened its default rule set. Instead of chasing the new violations one by one, I pinned the lint rule selection explicitly in config, so the build's behavior no longer depends on whatever a dependency decides is 'default' next."
- 2026-07-26 — "I mapped the three classic laundering stages — placement, layering, integration — directly onto which of my six detection subsystems catches each one, using real accounts from my own synthetic bank as the worked examples, not textbook abstractions."
- 2026-07-26 — "I generated a 10,000-customer synthetic population from calibrated distributions instead of a fixed dataset — lognormal income, weighted city and segment mix — and verified the output against my target distribution before building any transaction logic on top of it, so a realism bug couldn't hide inside a detection bug."
- 2026-07-26 — "My row-by-row insert took 2.3 hours for 200,000 rows. I benchmarked a temp-CSV-plus-COPY approach using tools I already had — same data, 4.5 seconds, 1,900x faster, zero new dependencies. I verified the values round-tripped correctly before trusting it with real data, which caught nothing — but I checked anyway, because 'faster' and 'correct' are two separate claims."
- 2026-07-26 — "Rewriting a ledger's running balances after a mid-history insert hit the exact same row-by-row UPDATE bottleneck I'd just fixed for INSERT the day before — so I generalized the fix into a reusable bulk_update helper, then went back and found the same slow pattern sitting untouched in code I'd written days earlier. Cut my test suite runtime by 5x as a side effect of fixing a correctness path, not a performance path."
- 2026-07-26 — "My first typology only ever touched one account, so I wrote its balance-recompute logic as single-account by default. Before writing the second typology — which by definition crosses multiple accounts — I extracted that logic into a shared function first, so the harder multi-account version was composition, not new code to debug."
- 2026-07-26 — "I found a rare-but-real bug in my own split algorithm before it ever shipped — a subtracted-remainder approach that could go negative under unlucky random draws — and replaced it with a proportional weighted split that's negative-proof by construction, then proved it with a 1,400-case stress test instead of trusting the math."
- 2026-07-26 — "I wrote a mathematical safety proof for a debit that could never overdraw an account, then stress-tested it anyway with 50 injections across 10 accounts — and it failed. The bug: my 'historical minimum' calculation only checked balances after transactions, missing the balance before the very first one. A proof and a test that only agree with each other prove nothing; they have to agree with reality."
- 2026-07-26 — "The right question wasn't 'how do I prove this debit is safe' — it was 'can I design the transaction order so the question never comes up.' Structuring credit before debit turned an entire class of overdraft bugs from something I had to prove into something that couldn't happen."
- 2026-07-26 — "My unit test suite had 67 passing tests, including a dedicated overdraft stress test, and none of them caught a real bug — because the stress test only covered business accounts, matching every other typology's pattern, and the actual bug needed NRI accounts plus multiple debit legs in one call to surface. It was my own 'real-world proof run,' not the test suite, that caught it. Now I treat the proof run as a test in its own right, not just a demo number."
- 2026-07-26 — "Every typology I built passed its own tests in isolation. Before calling the phase done, I ran a capstone that deliberately overlapped all six on the same accounts and re-verified the entire ledger, not just the parts each typology touched. It passed — which told me the safety properties I'd designed typology-by-typology actually composed, instead of just assuming they would. Then I kept that capstone as a permanent test, since 'works alone' and 'works together' are genuinely different claims that need separate proof."
- 2026-07-26 — "My first version of a money-laundering detection rule flagged 24 legitimate businesses out of 300 on a clean dataset. Instead of raising the amount threshold — which would have cost real recall — I found the channel the injected crime specifically used that ordinary business cash flow never does, and filtered on that. Real 10,000-customer proof: 93.3% recall, 100% precision, 0% false positives across 90 injected schemes, exceeding the ≥80%/<5% target — with one typology honestly reported at 60% recall and a concrete, defensible reason why, rather than hidden or averaged away."
- 2026-07-28 — "My name-screening engine hit 100% recall and 29% precision — and before I reported that as evidence of the industry's false-positive problem, I checked whether it was just my bug. Every false positive scored above 0.98, so none were sloppy matches; they were six customers genuinely sharing a listed PEP's name and thirty sharing a sanctioned person's transliteration. No algorithm separates those — that's what date of birth and nationality are for."
- 2026-07-28 — "I was asked to add Double Metaphone via jellyfish. It isn't in the library — jellyfish ships single Metaphone, Soundex and NYSIIS. So I benchmarked all of them against the actual transliteration pairs that mattered, found the specific case in the original code comment was missed by every phonetic algorithm and caught by Jaro-Winkler, and built JW-primary with Metaphone corroborating instead of adding a second dependency to satisfy the letter of the request."
- 2026-07-28 — "I nearly shipped a name matcher built on whole-string Jaro-Winkler. Testing it against deliberate impostors showed it scored two different people who merely shared a first name at 0.900 — above any usable threshold — because Jaro-Winkler weights shared prefixes heavily. Switching to token-by-token alignment dropped that pair to 0.73 while leaving genuine transliterations above 0.95: less code and more accurate at the same time."
- 2026-07-28 — "I published a 29% precision figure as evidence of AML's false-positive problem, then went back and tested whether my own synthetic data had manufactured it. Widening the name pool — one variable, same seeds — moved precision to 75% and took exact-name collisions to zero. So I could say precisely that 86% of my headline number was an artefact and 14% was the real effect. I'd rather own a smaller number I can defend than a dramatic one I can't."
- 2026-07-28 — "When I widened a dataset's name pool I appended the new values after the originals instead of replacing them. That one choice meant I could reproduce the old distribution by slicing the list, which turned 'how much of this result is my own data's fault?' into a controlled two-arm experiment instead of an unanswerable question."
- 2026-07-28 — "My graph layer reconstructed all 15 mule networks with no false positives — but the number I'd actually lead with is that five of my six laundering typologies were invisible to it, because their counterparties banked elsewhere and left no edge to analyse. Graph analytics only sees the fraction of a network that sits inside your own institution."
- 2026-07-28 — "I deleted a fan-in/fan-out detector I had just finished writing. Measured against a clean world it fired zero times, and when I loosened the thresholds until it fired, 72 of 76 hits were legitimate shops receiving customer payments. None of my typologies were fan-shaped, so it was decoration — I documented the measurement and removed the code rather than shipping a detector that had never detected anything."
- 2026-07-28 — "Pairing the two legs of a transfer on timestamp-and-amount looked obviously correct and silently cross-joined unrelated payments that happened to coincide. The fix was joining on the reference number both legs actually share. I only caught it because the edge count looked too high for the size of the world — the test would have passed either way."
- 2026-07-29 — "My supervised model hit a perfect average precision, so I went looking for the bug instead of celebrating. The synthetic world had never generated a legitimate cash or international transaction, so the model had learned 'cash equals crime'. I fixed the data — and that immediately broke my earlier rules engine, taking it from zero false positives to twenty-four, which told me its perfect precision had been an artefact too."
- 2026-07-29 — "The valuable output of my six-model tournament wasn't the winner, it was that the models fail differently: isolation forest caught 8 of 8 shell-company accounts but only 7 of 18 mule-network ones, while one-class SVM caught 18 of 18. A bank running a single model is blind wherever that model is weak, and its own metrics can't show it — you need ground truth broken down by crime type, which production data never has."
- 2026-07-29 — "I declined to add the last two models to my benchmark. A feature-importance audit showed three features carrying 98.6% of the signal, so the benchmark was measuring an artefact of my synthetic data rather than detection skill. Adding two more models to that would have produced two more inflated numbers instead of one honest blocker."
- 2026-07-29 — "I fixed my synthetic data and watched three of my four models get dramatically worse — one-class SVM fell from 0.91 to 0.22. That was the point: they had learned that anomalous means large, which only worked while my honest population had no big legitimate transactions. Real banks have an enormous upper tail, which is exactly why unsupervised anomaly detection underdelivers in production AML."
- 2026-07-29 — "I measured one of my own detection rules and concluded it was unfixable. Shell-company schemes sat at 6 payments and 50% counterparty concentration — inside the range of honest businesses with one dominant client. No threshold separates them, because in reality there's no difference. I documented it as producing genuine false positives rather than pretending a tuning pass would solve it."
- 2026-07-29 — "I implemented GraphSAGE in about fifteen lines of PyTorch instead of adding a graph-ML framework for a single layer — aggregate your neighbours' features, concatenate with your own, transform. Fewer dependencies, and I can explain every line to an auditor."
- 2026-07-29 — "I built a risk score combining four detection layers, measured it, and found it did not out-rank the best single layer — at one alert budget it was actively worse, because sanctions screening was diluting it. So I documented the combination's real value as explainability and tiering rather than accuracy, and split screening out as a separate queue."
- 2026-07-29 — "My graph layer produced 10 alerts and all 10 were real launderers — perfect precision but low reach. Rules gave more volume at 72%, ML the broadest at 60%. That's a tiered alert queue derived from measured evidence rather than an org chart: work the perfect-precision tier first, because scarce analyst hours should go where the hit rate is highest."
- 2026-07-29 — "I caught a bug in my own evaluation code that was understating recall threefold — it divided by every positive in the dataset while only ranking a held-out slice. I only noticed because I knew the split contained exactly 26 positives and the reported figure was impossible. A plausible wrong number is far more dangerous than an obviously broken one."
- 2026-07-29 — "The case store snapshots the evidence at the moment a case opens rather than re-deriving it later, because my detectors get retuned — I'd already changed two rules and knowingly left a third producing false positives. Without the snapshot, an analyst's decision from last month would silently acquire reasoning they never actually saw. That's the difference between an audit trail and a reconstruction."
- 2026-07-29 — "I made the case audit trail append-only and asserted it with a source-level test, not a convention. Every mutation — assign, note, close, reopen — writes an event, and reopening clears the disposition but never the record of having had one, because changing your mind is itself a fact a regulator is entitled to see."
- 2026-07-29 — "I mapped case-lifecycle violations to 409 Conflict rather than 500, because closing an already-closed case isn't a server fault — it's usually two analysts with the same queue open, and the UI needs to say 'someone already dispositioned this' rather than 'something broke'. I also refused to default the actor field on mutations: the case store rejects anonymous changes, and inventing a default would have created an audit trail that lies."
- 2026-07-29 — "My API serves the list of valid case dispositions from the case store rather than letting the frontend hardcode it. If the UI invented its own vocabulary the field would slowly stop meaning anything — and disposition statistics are exactly what a regulator samples."
- 2026-07-29 — "I built the alert queue, looked at it, and found my risk score had been hiding genuine cases. One rule firing counted as a third of a signal, so a confirmed structuring scheme — 27 sub-threshold cash deposits — scored 11.7 out of 100 and fell below the queue cut-off, while mule accounts appeared. A hundred and seventy-eight passing tests never caught it, because they all asserted relative behaviour and that stayed true. Some bugs only surface when a human looks at an absolute number next to a threshold."
- 2026-07-29 — "My alert queue is tiered by kind of evidence rather than sorted by one blended risk number, because I'd measured that the blended score never out-ranked the best single detector while graph evidence alone hit 100% precision. The UI follows the measurement, not the original plan."
- 2026-07-29 — "Building the entity-360 screen caught the same class of bug two slices running. The transaction endpoint defaults to the latest hundred rows, so an account alerted for eighty-nine cash deposits displayed a statement that started a week after the account did — the evidence screen was truncating the evidence, and nothing failed, because the API did exactly what it was asked."
- 2026-07-29 — "I refused to compute the account totals from the transactions already loaded in the browser and did them in SQL over the whole history instead. Summing what's on screen is shorter code and would have under-reported every busy customer without ever looking wrong. A number derived from a window must never be presented as a number about the whole."
- 2026-07-30 — "My graph layer detected mule chains but an analyst couldn't act on one, because a chain is an assertion until you can name the transactions behind it. The fix was already in my own code — the SQL that pairs the two legs of a transfer had always selected both row ids and then thrown them away. When a result feels unverifiable, check whether the derivation already computed the proof and dropped it."
- 2026-07-30 — "Closing an already-closed case in my workbench tells the analyst 'someone else may have worked this case' rather than showing an error. That's only possible because three slices earlier I'd mapped lifecycle violations to 409 rather than 500 — the distinction between 'you broke it' and 'the record moved under you' has to be decided at the layer that knows the truth. By the time you're writing the UI message, if the API returned 500 for everything, the information is gone."
- 2026-07-30 — "I deliberately did not use an LLM for the SAR narrative, even though my own plan allowed it. A suspicious activity report is a regulatory filing where every figure is asserted to a regulator by the bank — a generated sentence that rounds a number is a false statement in a legal document, not a style problem."
- 2026-07-30 — "The first SAR narrative I printed caught a bug the whole project had been carrying: a confirmed structuring scheme with fifty cash deposits totalling thirty-three lakh described itself as 'low risk'. Measuring the bank, every case was low or medium and the top score that existed anywhere was 43.5 — 'high' and 'critical' were words describing nothing. My bands assumed a 100 that required all four detection layers firing at once on one account."
- 2026-07-30 — "Explainability in an AML stack isn't one property, it's four different ones. My graph layer can name the exact ledger rows behind its alert; my rules emit a reason string, screening answers an identity question, and the models emit a score. So the SAR annex is ranked by value and says so — calling a value ranking 'the evidence' would invent a link the detectors never produced."
- 2026-07-30 — "Building the investigator's workbench found three bugs in detection work that 190 passing tests hadn't: a scoring curve hiding single-rule cases below the queue cut-off, a statement truncating the evidence it existed to display, and risk bands calibrated to a score nothing could reach. All three surfaced by putting a number in front of a person next to a decision."
- 2026-07-30 — "I found my system was discarding fourteen of fifteen sanctions matches. A screening-only alert scored the layer's weight times the match confidence — 0.20 × 0.98 — landing just under the threshold at which a case opens, so only a perfect 1.000 name match ever reached an analyst. Every transliteration, the whole reason you run a fuzzy matcher, was thrown away one layer above the matcher that correctly found it."
- 2026-07-30 — "The same defect turned up four times in my risk aggregation: a global threshold applied to a score whose scale depends on which detection layers happened to fire. Every individual number looked reasonable; the bug only existed in the interaction. If a threshold and a weight get chosen in different places at different times, something is already broken and nothing will tell you."
- 2026-07-30 — "I stopped picking my case-opening threshold and derived it: above what a model alone can score, because a model-only alert has no reason to give an analyst, and at or below the faintest thing any control is willing to assert. Then I pinned that window with a test — which caught, on its first run, the same scoring bug hiding in my graph layer, where it had survived three slices because every chain in my demo world happened to be the same length."
- 2026-07-30 — "Under one shared alert budget my sanctions hits scored lowest of anything and were cut first — 42 of 92 eligible accounts didn't fit. That's exactly why banks run screening as its own queue with its own clock: a sanctions match is a blocking obligation, not a review-when-you-get-to-it item. I understand that as arithmetic now, not as organisational trivia."
- 2026-07-30 — "Before the last phase I audited my own project for claims I couldn't reproduce, and found CI had been green while running 178 of 226 tests. It installed only base dependencies, so three test files skipped silently — and a skipped module counts as one skip, so forty-eight missing tests hid behind the number 3. Two were the tests enforcing my most important invariant: detection code never reads ground truth. A control that's well designed, documented as enforced, and not executing is exactly what control testing exists to catch."
- 2026-07-30 — "My CI now fails if any test skips at all. With every optional dependency installed there's no legitimate reason to skip, so a skip can only mean something stopped resolving. A guarantee you have to remember to verify isn't a guarantee."
- 2026-07-30 — "The project claimed it shipped a visual artifact per phase, and no code in the repo could draw one — the charts had been generated in a session and lost. I rewrote them as self-contained SVG built on the scoring modules, so a chart can never disagree with the precision figure it's illustrating. A number in a chart and a number in a report that come from different code paths will eventually diverge."
- 2026-07-30 — "I was asked to add adverse media to my risk score and measured it first, which is how I ended up recommending against it. Of twenty-one accounts it flagged, one was actually laundering — and the set of laundering accounts only adverse media reached was empty. A signal adds recall only through accounts it alone reaches, so with that set empty no weighting can add a true positive; it can only reorder the queue, and reordering a capped queue pushes something real out. It cost two confirmed structuring cases."
- 2026-07-30 — "Adverse media was good at its own job — 47.6% precision identifying the right person, 100% recall — and useless for ranking laundering risk, because those are different questions. It ships on the entity screen as context for the analyst and carries zero weight in the score. That's where adverse media belongs in a real bank too: enhanced due diligence, not transaction monitoring."
- 2026-07-30 — "Measuring adverse media found something worse than adverse media: my ranking had no tie-break, and every single-rule case scores exactly 21.00, so forty-five accounts sat on one value with the alert budget's cut inside the cluster. Which twenty-four of forty-five an analyst worked was decided by dictionary insertion order. I caught it because the baseline moved between two runs of the same command."
- 2026-07-30 — "When I made ties deterministic the baseline settled at a third value — all three were equally arbitrary. So I stopped quoting the queue table as the result and built the recommendation on a statement that doesn't depend on the tie order, the budget or the weighting. A measurement whose headline moves when you change something irrelevant isn't a measurement yet."
- 2026-07-30 — "I nearly shipped an alternative that looked free — identical to baseline on every figure at the budget I'd picked. A budget sweep killed it: at a looser budget it inherited the screening layer's full weight and opened cases on news alone, eleven extra reviews for zero extra finds. One budget is not a measurement."
- 2026-07-30 — "I built a red team that mutates its own injector parameters generation over generation against my rules engine, and the finding wasn't one decay curve — it was that decay isn't uniform. Shell-company detection collapsed to zero recall in two generations and stayed there, confirming a rule I'd already flagged as structurally unfixable. Two other rules never fully converged across eight generations even at extreme parameter values. Averaging those into one Phase 3 recall number was hiding that some detectors decay fast and some don't decay at all."
- 2026-07-30 — "Building the red team benchmark, I found a harness bug that was hiding my own result: two typologies sometimes landed on the same account within one generation, and the cross-contamination made shell-company recall look like 8% when the real number was 70%. I fixed it by carving disjoint account pools that raise an error rather than silently sampling with replacement when they run low — a silent fallback there would have recreated the exact bug quietly instead of loudly."
- 2026-07-30 — "My benchmark's own 'converged' label used to imply permanent evasion once a typology hit zero recall. It doesn't — the adversary's parameters freeze, but each generation still runs against a freshly generated world, so a frozen doctrine can still get unlucky. I added a post-convergence stability metric and it caught that two typologies both labelled 'converged at generation 2' were not equally caught — one stayed at 0%, the other bounced back to 20%."
