# LaunderLab — project operating doc

Read this file + the last Field Notes entry at "start day".
Full 18-week plan: `../LAUNDERLAB-PLAN.md` (CareerForge root).
FCC concepts: `ledger/FCC-PRIMER.md` — the three laundering stages mapped to subsystems.

## Research thesis — three unsolved problems we attack

1. **Detection decay** — how fast does a detection stack rot against an adapting adversary? (Phase 8 benchmark; exists nowhere today)
2. **False-positive economics** — cost-per-true-alert measured for every detection config, not guessed (Phases 3–6)
3. **The cross-bank blind spot** — quantify what single banks can't see in multi-bank mule chains, then measure the lift from privacy-preserving co-operation (Phase 8.5; BIS Project Aurora does this privately — no open version exists)

**Three tracks:** an analyst doesn't just generate data, they investigate it. Track A —
Simulate (phases 0–2). Track B — Detect (phases 3–6). Track C — Investigate (phase 7).

## Quality bar (non-negotiable)

- `main` never breaks: every slice runs end-to-end before commit
- pytest + ruff green before every commit; GitHub Actions CI on every push
- every phase ships a **visual artifact**, never just tables
- blue-team code must never read `scheme_labels` (ground truth is for scoring only)

## Roadmap

- [x] **0.1** — repo scaffold, DuckDB ledger schema (4 tables + ground-truth labels), 5 tests, CI, demo command *(2026-07-22)*
- [x] **0.2** — seed loader: 25-customer cast (salaried/business/student/NRI/merchant) + one believable week of life, balance-reconciled, deterministic *(2026-07-22)*
- [x] **0.3** — statement generator v0: any account renders as an HTML bank statement, opening balance derived, `python -m launderlab statement <id>` opens it in browser *(2026-07-23)*
- [x] **0.4** — FCC primer doc: placement → layering → integration mapped to subsystems, grounded in the seeded cast *(2026-07-26)*
- [x] **1.1** — population generator: 10k customer profiles from distributions (not hand-typed), deterministic, segment mix within 0.5pt of target *(2026-07-26)*
- [x] **1.2** — transaction generator at scale: generalized seed.py's patterns over any population, bulk CSV+COPY loader (needed — executemany didn't finish 200k rows in 10 min, COPY does it in 4.5s); real 10k-customer/30-day run: 630,755 transactions in 31.3s, all balances reconciled, all 10,000 accounts active *(2026-07-26)*
- [ ] **1.3** — realism polish backlog (weekday/weekend variation, holidays, more diversity) — parallel/non-blocking, per Dhanush 2026-07-26
- [ ] **Phase 1** (wks 1–2) — World Engine: 10k customers, behavior profiles, clean-traffic realism + histogram visual — engine proven at scale (1.2); polish (1.3) demoted to backlog, not blocking Phase 2
- [x] **2.1** — typology injection engine + structuring: injects into an already-generated account's history, rewrites balance_after globally correct, writes ground truth to `scheme_labels`; proof run — 20 schemes into the 10k-customer world, ₹2.55cr placed, 397 labels, full ledger reconciliation verified *(2026-07-26)*
- [x] **2.2** — mule network / layering: money hops through a chain of accounts (source → mules → sink), each hop skims 3-8%, timestamps strictly increasing; extended balance recompute to multiple accounts per scheme; proof run — 15 schemes into the 10k-customer world, ₹1.41cr entered, 107 labels, global reconciliation verified after touching ~60 accounts *(2026-07-26)*
- [x] **2.3** — shell company / trade-based layering: few large "invoice" payments concentrated from one shell counterparty into a real business, sequential invoice numbers; proof run — 15 schemes into the 10k-customer world, ₹3.89cr placed, 84 labels, global reconciliation verified *(2026-07-26)*
- [x] **2.4** — round-tripping: money leaves an account and returns to the SAME account inflated 2-10%; safety cap derived from the account's true minimum-ever balance (opening included, not just post-transaction balances) so an injected departure can never overdraw regardless of where in the timeline it lands; proof run — 15 schemes into the 10k-customer world, ₹3.8L departed/₹4.0L returned, global reconciliation verified *(2026-07-26)*
- [x] **2.5** — dormant-account reactivation: a low-activity account's real last-transaction date is found, then a sudden large credit + rapid cash-out (ATM/UPI) is injected after a quiet gap; safe by construction (credit always precedes debit, like mule_network); proof run — 15 schemes into the 10k-customer world, ₹2.09cr credited, 56 labels, global reconciliation verified *(2026-07-26)*
- [x] **2.6** — high-risk geography: international remittances tied to FATF-style blacklist/greylist jurisdictions; caught a real overdraft bug via a wider stress test (business+NRI accounts, multiple debit legs per call) that the narrower business-only test missed — fixed with a shared debit budget across a call's legs instead of an independent per-row cap; promoted `ledger.account_true_minimum()`/`safe_debit_ceiling()` to shared utilities (now used by 2 typologies); proof run — 15 schemes into the 10k-customer world, 28 labels (10 blacklist/18 greylist), global reconciliation verified *(2026-07-26)*
- [x] **Phase 2** (wks 3–4) — Typology Injector, all 7 typologies complete: structuring, layering, mule networks, shell companies, round-tripping, dormant-account activation, high-risk geography — each labels ground truth for later precision/recall/false-positive scoring *(2026-07-26)*
- [x] **2.7 — Phase 2 capstone** — all six typologies injected together onto overlapping accounts (the SAME business accounts run structuring + shell company + round-tripping simultaneously), proving the engine composes correctly, not just that each typology works alone; permanent regression test added; proof run — 60 schemes, 414 ground-truth labels, 10 accounts confirmed running multiple crime types at once, full-ledger reconciliation verified across 631,169 transactions *(2026-07-26)*
- [x] **Phase 3** (wks 4–5) — Rules engine: 6 tunable detection rules (structuring_burst, rapid_pass_through, round_trip, counterparty_concentration, dormancy_burst, high_risk_geography), scoring module (precision/recall/FP-rate vs ground truth, never read by rules.py), tuning workflow (each rule's thresholds tuned against a clean world + injected cases). Acceptance bar (≥80% recall, <5% FP rate) exceeded in real 10k-customer proof: **93.3% recall, 100% precision, 0% false-positive rate** across 90 injected schemes. One honest gap: dormant_reactivation at 60% (9/15) — explained, not hidden, in FIELD-NOTES *(2026-07-26)*
- [x] **Phase 4** (wk 6) — Screening: `screening/matcher.py` (Jaro-Winkler + Metaphone over aligned name tokens, replacing the difflib first draft), `screening/inject.py` (plants watchlist entities as exact/transliteration/initials/reordered variants + synthetic adverse media, with `entity_labels`/`media_labels` ground truth), `screening/engine.py` (both legs over the whole bank), `screening/scoring.py` (precision/recall — the only module allowed to read the labels). MCP `screen_name` rewired to call the shared matcher, `adverse_media_check` tool added. Real 10k proof: **100% recall on both legs; entity precision 75.0%, adverse media 15.8%** (after slice 4.2 fixed the name pool — the original narrow-pool run scored 29.4% / 3.7%). Zero sloppy matches in either arm: every FP scored ≥0.88 and is a genuine name variant *(2026-07-28)*
- [x] **4.2 — name-collision experiment** (done before 4.1, because it re-scoped it): widened `population.py`'s pool from 40×25 to 193×120 (1,000 → 23,160 combinations) and re-ran Phase 4's proof as a controlled two-arm test — same world seed, same injection seed, only name diversity varying. **86.1% of screening false positives were collision density; 13.9% is irreducible name ambiguity.** Entity precision 29.4% → **75.0%**, media 3.7% → **15.8%**, recall stayed 100% in both arms *(2026-07-28)*
- [ ] **4.1** — secondary-identifier disambiguation (DOB/nationality). Re-scoped by 4.2: with the realistic name pool the exact-same-name false positives went 6 → **0**, so the remaining 5 FPs are transliteration-equivalents, not identical names. Smaller prize than it looked — do it for realism, not for the precision number
- [x] **Phase 5** (wks 7–8) — Graph analytics: `graph/build.py` (pairs DR/CR legs back into directed transfers via the shared narration reference), `graph/motifs.py` (pass-through chain detection with suffix collapsing), `graph/scoring.py` (scorer-only). Real 10k proof: **15/15 mule networks reconstructed, 15 chains reported, 0 false positives — 100% precision and 100% recall**, on a graph of 8,538 nodes / 173,525 edges built in 1.8s. Headline finding is the blind spot: **only 1 of 6 typologies is visible to a graph at all** — the other five have counterparties outside the bank, so they leave single legs and no edges *(2026-07-28)*
- [x] **6.1** — ML harness + 4 of 6 families: `ml/features.py` (31 behavioural features, label-free), `ml/dataset.py` (labels + stratified split), `ml/models.py` (gradient boosting, isolation forest, one-class SVM, autoencoder), `ml/tournament.py` (leaderboard scored at an **alert budget**, since that is how a bank actually operates). 9 new tests. **Found and partly fixed a world-realism leak** — see 6.2 *(2026-07-29)*
- [x] **6.2 — amount realism** — added legitimate large payments: business settlements/loans (₹2-25L), personal big-ticket events modelled as a funding credit then the purchase it pays for (property, vehicle, FD, wedding), and NRI property remittances. Top-3 feature share fell **98.6% → 50.9%**, `std_amount` lost its 67.8% dominance. Forced honest re-tuning of two Phase 3 rules *(2026-07-29)*
- [x] **6.3 — LSTM + GraphSAGE** — the last 2 families, hand-implemented in plain PyTorch (CPU build, ~200MB) rather than adding torch-geometric for one layer. All six families now run on one leaderboard *(2026-07-29)*
- [ ] **Phase 6** (wks 8–10) — ML tournament: 6 algorithm families on one leaderboard, all with explainability; later scored on decay vs the red team
- [ ] **7.1 — risk aggregation (build FIRST, before any workbench UI)** — combine Phase 3's rule alerts + Phase 4's screening scores + Phase 5's graph flags + Phase 6's 6 ML model scores into one risk number per account, traceable back to which signals fed it. Deliberately deferred until now: the right approach (weighted sum vs. small meta-model vs. max-signal) depends on real score distributions Phases 3–6 haven't produced yet. This is the one point in the project where combining signals is actually necessary — nothing before Phase 7 needs one ranked number.
- [ ] **Phase 7** (wks 10–12) — Investigator workbench (FastAPI + React): alert cards with risk score (from 7.1), "why" bullets, AI-generated case summary
- [ ] **Phase 8** (wks 12–14) — Red team co-evolution engine → detection-decay benchmark
- [ ] **Phase 8.5** (wks 15–16) — Multi-bank experiment: quantify the cross-bank blind spot, prototype privacy-preserving sharing, measure lift
- [ ] **Phase 9** (wks 17–18) — Story Mode + whitepaper + demo video + launch

## Slice log

| date | slice | what shipped |
|------|-------|--------------|
| 2026-07-22 | 0.1 | ledger schema (customers, accounts, transactions, scheme_labels), 5 passing tests, CI, `python -m launderlab` demo |
| 2026-07-22 | 0.2 | world seed: 25-person cast, one week of life (~salaries, rent, EMIs via NACH, UPI P2P, merchant footfall, business receipts, GST), two-leg internal payments, no-overdraft rule, 5 new tests |
| 2026-07-23 | 0.3 | statement generator v0 (`statement.py`): HTML statement per account, derived opening balance, CLI `statement <id>` opens in browser; sped up test suite 330s→130s by sharing seeded fixtures instead of reseeding per test; 4 new tests |
| 2026-07-26 | fix | CI broke on ruff 0.16.0's wider default rule set (unrelated to any diff); pinned `[tool.ruff.lint] select` explicitly, verified against 0.16.0 locally |
| 2026-07-26 | 0.4 | `ledger/FCC-PRIMER.md`: placement/layering/integration mapped to subsystems and phases, examples grounded in the seeded cast — Phase 0 complete |
| 2026-07-26 | 1.1 | `world/population.py`: procedural generator for 10k customer profiles (5 segments, lognormal income, weighted cities, deterministic RNG), 6 new tests; verified against a real 10k run (segment mix within 0.5pt of target, median salaried income ₹54,000) |
| 2026-07-26 | 1.2 | `world/generate.py`: generalized seed.py's event patterns (salary/rent/EMI/P2P/merchant/business) over any population; added `ledger.bulk_insert()` (temp CSV + DuckDB COPY, no new dependency) after measuring executemany took 8,224s (2.3h) for 200k rows while COPY does it in 4.5s (1,900x); 8 new tests; real 10k-customer/30-day run: 630,755 transactions in 31.3s, ₹274cr moved, 0 negative balances, all 10,000 accounts active, channel mix 89% UPI |
| 2026-07-26 | 2.1 | `typology/structuring.py`: injects a structuring scheme into an existing account's history, rewriting balance_after via a new `ledger.bulk_update()` (set-based UPDATE...FROM — the UPDATE-side fix for the same executemany overhead) and labeling ground truth in `scheme_labels`; retrofitted `seed.py` onto `bulk_insert` too, cutting the full test suite from ~150s to 29s; 12 new tests (36 total); real proof: 20 schemes injected into the 10k-customer world, ₹2.55cr placed, 397 ground-truth labels, full-ledger reconciliation verified after injection |
| 2026-07-26 | 2.2 | `typology/mule_network.py`: layering typology — money hops through a chain of accounts (source→mules→sink), 3-8% skimmed per hop, timestamps strictly increasing; extracted `ledger.recompute_account_balances()` from structuring.py (now shared, since layering touches multiple accounts per scheme); 8 new tests (44 total); real proof: 15 schemes into the 10k-customer world, ₹1.41cr entered, 107 ground-truth labels, global reconciliation verified after touching ~60 accounts across 15 schemes |
| 2026-07-26 | 2.3 | `typology/shell_company.py`: integration typology — a handful of large invoice payments concentrated from one shell counterparty (reused mule_network's SHELL_NAMES pool) into a real business, sequential invoice numbers; weighted-split algorithm (not subtracted remainder) proven never-negative via a 1,400-case stress test across seeds/n; 6 new tests (50 total); real proof: 15 schemes into the 10k-customer world, ₹3.89cr placed, 84 labels, global reconciliation verified |
| 2026-07-26 | 2.4 | `typology/round_tripping.py`: money leaves an account and returns to the same account inflated; caught a real overdraft bug via a 50-injection stress test — the safety cap was based on min(balance_after) which ignores the balance *before* the first transaction, so an injected departure landing earlier than every existing row could overdraw against the true opening balance; fixed by including opening balance in the safety floor; 5 new tests (55 total); real proof: 15 schemes into the 10k-customer world, ₹3.8L departed / ₹4.0L returned, global reconciliation verified |
| 2026-07-26 | 2.5 | `typology/dormant_reactivation.py`: sudden large credit + rapid ATM/UPI cash-out injected after a real quiet gap on a low-activity account; promoted shell_company's split helper to a shared `split_uneven()` (now used by two typologies); 6 new tests (61 total); real proof: 15 schemes into the 10k-customer world, ₹2.09cr credited across 56 labels, global reconciliation verified |
| 2026-07-26 | 2.6 | `typology/high_risk_geography.py`: international remittances tagged with a synthetic FATF blacklist/greylist country; promoted round_tripping's overdraft-safety logic into shared `ledger.account_true_minimum()`/`safe_debit_ceiling()`; caught and fixed a real overdraft bug the business-only unit test missed but a business+NRI scale run caught — independent per-row debit caps within one call could sum past the safe ceiling, fixed with a shared running budget; 6 new tests (67 total) with a strengthened stress test; real proof: 15 schemes into the 10k-customer world, 28 labels (10 blacklist/18 greylist), global reconciliation verified. **All 7 Phase 2 typologies complete.** |
| 2026-07-26 | 2.7 | `tests/test_phase2_capstone.py`: all six typologies injected together onto deliberately overlapping accounts (5 business accounts run structuring + shell_company + round_tripping simultaneously) — a permanent regression test for cross-typology composability, the exact class of bug found on Days 9/10/12; 5 new tests (72 total); real proof at 10k scale: 60 schemes, 414 ground-truth labels, 10 accounts confirmed running multiple crime types at once, full-ledger reconciliation verified across 631,169 transactions |
| 2026-07-26 | Phase 3 | `detect/rules.py` (6 tunable rules) + `detect/scoring.py` (precision/recall/FP-rate vs ground truth — the only module allowed to read scheme_labels). Tuning story, found by testing not guessing: round_trip's amount threshold missed a real case (safety-capped departure was below the floor) — fixed by trusting the RTGS-channel filter to carry precision instead of a high amount floor; counterparty_concentration's 60% threshold missed a 56%-concentration real case — lowered to 50%; dormancy_burst's naive "big number after a gap" rule falsely flagged a real weekly-pocket-money student — redesigned around the actual injected signature (rapid cash-out after the gap, not just gap+size) which eliminated it, then a longer/stricter gap requirement eliminated a second wave of business-cycle false positives it introduced. 12 new tests (84 total). Real 10k-customer proof: **90 schemes injected (15 per typology), 93.3% overall recall, 100% precision, 0% false-positive rate** — acceptance bar (≥80% recall, <5% FP) exceeded. One typology underperforms honestly: dormant_reactivation at 60% (9/15), because the injector's own gap parameter sometimes lands too close to normal weekly cadence to distinguish — a genuine, explained precision/recall tradeoff, not a hidden gap. Real visual: detection-rate-per-typology bar chart. |
| 2026-07-28 | 4.0a / 7 (early) | `mcp_server.py`: MCP server exposing the ledger + rules engine to an AI agent — 5 read-only tools (`screen_name`, `customer_profile`, `transaction_history`, `run_detection`, `audit_trail`) over stdio. Reuses `detect.rules.run_all()` rather than reimplementing detection. Fuzzy screening uses stdlib `difflib` (no new matching dependency); caught the transliteration case `Farhan Ali` → watchlist `Farhaan Ali` at 0.952. Three deliberate boundaries: read-only against the ledger, every call (failures included) written to an append-only `audit_log` table via a decorator so no tool can run unlogged, and **no generic SQL tool** — one would let any agent read `scheme_labels` and invalidate every score the range produces (test asserts this, mirroring `test_rules_never_reference_scheme_labels`). 11 new tests (95 total), ruff clean, verified end-to-end over real MCP stdio. **Splits across two phases:** `screen_name` is a partial **Phase 4** delivery (sanctions/PEP fuzzy matching + high-risk geography — the adverse-media leg is still missing, and difflib is edit-distance only where Phase 4 proper likely wants phonetic matching + a precision/recall score against ground truth, the way Phase 3 was graded). The MCP interface itself is **Phase 7** (investigator workbench) territory, built ~3 phases early. **Not demo-ready:** `run_detection` returns 0 alerts against the 25-customer seed world because no schemes are injected there; it needs a generated+injected world behind it before it shows anything. Watchlist (`db/watchlist.json`) is synthetic — swap via `LAUNDERLAB_WATCHLIST` for OFAC SDN / UN Consolidated before any real conclusion. |

| 2026-07-28 | Phase 4 | `screening/` package: `matcher.py` (Jaro-Winkler + Metaphone over aligned name tokens), `inject.py` (entity + adverse-media ground truth), `engine.py` (both legs, bank-wide), `scoring.py` (the only module allowed to read `entity_labels`/`media_labels`). Three measured design decisions, none guessed: (1) **whole-string Jaro-Winkler was dropped entirely** — its prefix bonus scored "Suresh Kumar" vs "Suresh Gupta" at 0.900, above any usable threshold; token alignment drops that same pair to 0.73 while leaving real transliterations at 0.95+; (2) **Metaphone earns its place on one specific case** — mohammed/muhammad is phonetically identical but only 0.850 on Jaro-Winkler, so it needs the phonetic floor; (3) **Double Metaphone is not in jellyfish** (only `metaphone`/`soundex`/`nysiis`/`match_rating_codex`), and the `nguyen/nuyen` case the ponytail comment named is missed by *every* phonetic algorithm but caught by Jaro-Winkler at 0.950 — so JW is primary and Metaphone corroborates, rather than adding a second phonetics dependency. MCP `screen_name` rewired to the shared matcher (test asserts no second copy of the logic), `adverse_media_check` added. 45 new tests (129 total — 29 screening, 16 MCP). Real 10k proof: entity screening **100% recall, 29.4% precision** (all four variant kinds caught); adverse media **100% recall, 3.7% precision**. Every FP scored ≥0.986 — zero sloppy matches; 6 were the *exact* listed name and 30 were transliteration-equivalents, none separable without a secondary identifier. Honest caveat recorded: the world's 1,049 distinct names across 10,000 customers exaggerates collision density. |

| 2026-07-28 | 4.2 | Name-collision experiment. Widened `world/population.py` from 40×25 to 193×120 names (1,000 → 23,160 combinations), keeping the originals at the front of each list so a *controlled* before/after was possible — same world seed, same injection seed, only name diversity varying. Narrow arm: 9.41 customers per name, entity precision 29.4%, media 3.7%, 36 FPs (6 exact-name, 30 fuzzy). Wide arm: 1.49 customers per name, entity precision **75.0%**, media **15.8%**, 5 FPs (**0 exact-name**, 5 fuzzy). Recall stayed 100% on both legs in both arms — diversity cost nothing. **Decomposition: 86.1% of the false-positive rate was collision density in the generated world; 13.9% is irreducible name ambiguity.** This converts Phase 4's caveat into a measurement and re-scopes slice 4.1, since the same-name FPs it was meant to fix have gone to zero on their own. Honest note: the prediction before running was 60–80% entity precision (actual 75.0%, good) and 30–40% media precision (actual 15.8%, too optimistic — adverse media stays noisy because each trap article still name-matches ~4 customers). |

| 2026-07-28 | Phase 5 | `graph/` package: `build.py`, `motifs.py`, `scoring.py`. Edge reconstruction joins on the **reference number both legs share in the narration**, not on (timestamp, amount) — the latter cross-pairs unrelated coincident transfers and produced visibly wrong edges on a 400-customer world. Chain detection walks forward through time: a hop qualifies when money leaves within 36h carrying 80–100% of what arrived, i.e. the account kept a cut rather than spending it; windows sit deliberately outside the injector's own 3–8%/2–30h constants so detection isn't tuned to its own simulator. Added suffix collapsing after finding 5 planted chains reported as **13** — growing from every edge rediscovers a long chain from its 2nd and 3rd accounts, and an investigator wants the path once, not four overlapping views. **Fan-in/fan-out were built and then removed**: measured against this world they fired 0 times at usable thresholds, and loosening until they fired produced 72/76 merchants — legitimate shops receiving many small payments, i.e. pure false positives. No injected typology is fan-shaped, so shipping them would have been decoration. 9 new tests (138 total). Real 10k proof: 15/15 layering schemes reconstructed, **100% precision, 100% recall**, 0 FPs; graph of 8,538 nodes/173,525 edges in 1.83s, detection in 0.21s. **Blind spot measured: layering 15/15 visible; structuring, shell_company, round_tripping, dormant_reactivation, high_risk_geography all 0/15** — their counterparties bank elsewhere, so no edge exists to analyse. Phase 3's `rapid_pass_through` flagged 47 of 62 layering accounts individually but reconstructed no path; the graph is what turns flagged accounts into a chain. |

| 2026-07-29 | 6.1 | `ml/` package: 31 label-free behavioural features, stratified split, 4 model families, leaderboard scored at an alert budget (top-100 accounts) rather than an arbitrary threshold — the only fair way to compare a calibrated probability against an autoencoder's reconstruction error, and how a bank actually works. **The result was a leak hunt, not a leaderboard.** Gradient boosting scored AP 1.000 on the first run; the cause was that the legitimate world produced **zero CASH and zero INT transactions**, so both channels appeared only via injected crime and the model learned "channel = CASH means crime". Fixed the world twice: inward remittances now use INT (they are international), and merchants *and* businesses now bank real cash takings in structuring's own size band. **That broke Phase 3** — `structuring_burst` went 0 → 24 false positives on a clean world, proving its earlier 100% precision was an artefact of a world with no legitimate cash. Re-tuned against measured distributions (legit tops out at 21 deposits / ₹1.31M; structuring runs 27-47 / ₹1.8M+) and encoded the honest cost as a test: **small structuring schemes are now a documented blind spot**, indistinguishable from a shop banking takings. Final 10k leaderboard: gradient_boosting AP 1.000, one_class_svm 0.910, autoencoder 0.717, isolation_forest 0.395. Per-typology breakdown is the real result — isolation forest catches shell_company 8/8 and structuring 5/5 but only 7/18 layering, so **the families fail differently**, which is the argument for a tournament. Leak audit kept next to the number: 3 features carry 98.6% of signal (`std_amount` 67.8%), so the supervised score is not yet a credible AML result — tracked as 6.2. |

| 2026-07-29 | 6.2 + 6.3 | **Amount realism, then the last two models.** 6.2 gave the world its missing upper tail: business settlements/loans (₹2-25L), personal big-ticket events modelled as a funding credit (FD maturity, loan disbursal, bonus, sale proceeds) followed 3-10 days later by the purchase it pays for, and NRI property remittances. Modelling the funding leg mattered twice — a lump-sum debit alone would have been refused by the no-overdraft rule and never appeared, and the credit is what stops "a large incoming payment" being an automatic crime signal. Effect on the leak: top-3 feature share **98.6% → 50.9%**, `std_amount` lost its 67.8% dominance (top feature is now `round_amount_ratio` at 24.7%), and legitimate traffic now includes 464 transactions over ₹5L with a max of ₹29L. **Unsupervised scores collapsed** — one-class SVM 0.910 → 0.219, autoencoder 0.717 → 0.375, isolation forest 0.395 → 0.155 — because they had been riding the same artefact: "anomalous" had meant "large". Supervised held at 0.925. Two more Phase 3 rules needed honest re-tuning: `counterparty_concentration` was measured to be **unfixable** (shell schemes sit at 6 payments/50% concentration, inside the legitimate 5-9/51-63% range — an honest supplier with one big client is indistinguishable from a shell-fed front), so `min_count` went 2 → 4 to kill single-payment FPs and the clean-world test now asserts a bounded alert load rather than silence. 6.3 added `ml/deep.py`: an LSTM over each account's ordered transaction history and a GraphSAGE GNN, both hand-written in plain PyTorch (~15 lines for the SAGE layer) rather than adding torch-geometric for one layer; CPU-only torch (~200MB vs ~2.5GB CUDA) since these models are tiny. Adjacency is built per-split so a test account can never aggregate a training account's features. 16 new tests. All six families now on one leaderboard. |

## Pending / next on this thread

- **Make the MCP server demo-worthy**: point it at a generated 10k world with the Phase 2 typologies injected *and* Phase 4's entities/media planted, so `run_detection`, `screen_name` and `adverse_media_check` all surface real hits. Until then it demos as empty lists and undersells the work.
- **Secondary-identifier disambiguation** (slice 4.1 above) — now a realism item rather than a precision fix, since 4.2 measured the exact-name collisions away.
- Consider whether S4's investigator workbench builds on these tools or replaces them — the tool surface was designed for an agent, not for a UI.

## Rituals

- **"start day"** — read this + last Field Notes entry → agree one slice → build it
- **"close day"** — commit + push, append 3 insights to `ledger/FIELD-NOTES.md` (🏦 FCC, 🔧 engineering, 🎯 interview line)
- **Sunday** — red-vs-blue tournament, metrics snapshot, `/code-review` on the week's diff
- Phase completion → resume bullet into `job hunt/db/profile.md` + LinkedIn post
